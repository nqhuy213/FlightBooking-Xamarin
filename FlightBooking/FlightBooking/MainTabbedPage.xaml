<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
            
            xmlns:views="clr-namespace:FlightBooking.Views"
             x:Class="FlightBooking.MainTabbedPage">
  <!--Pages can be added as references or inline-->
    <views:CustomNavigationView Title="Search" IconImageSource="{StaticResource AirplaneIcon}">
        <x:Arguments>
            <views:MainView/>
        </x:Arguments>
    </views:CustomNavigationView>
    <views:CustomNavigationView Title="Boarding Pass" IconImageSource="{StaticResource BoardingPassIcon}">
        <x:Arguments>
            <views:BoardingPassView/>
        </x:Arguments>
    </views:CustomNavigationView>
    <views:CustomNavigationView Title="Check-in" IconImageSource="{StaticResource CheckinIcon}">
        <x:Arguments>
            <views:CheckInView/>
        </x:Arguments>
    </views:CustomNavigationView>
    <views:CustomNavigationView Title="More" IconImageSource="{StaticResource MoreIcon}">
        <x:Arguments>
            <views:MoreView/>
        </x:Arguments>
    </views:CustomNavigationView>
</TabbedPage>